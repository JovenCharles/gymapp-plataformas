services:
  frontend:
    image: jovencharles/gymaster-frontend:latest
    container_name: gymaster-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro